---
phase: 01-navigation-site-structure
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/app/portfolio/page.tsx
  - src/app/cursos/page.tsx
  - src/app/espaco/page.tsx
  - src/app/servicos/page.tsx
autonomous: true
---

<objective>
## Goal
Create route placeholders for all planned pages (Portfolio, Cursos, Espaço, Serviços) so that navbar links resolve and each page has a branded "Coming Soon" state.

## Purpose
Navbar links to /portfolio, /cursos, /espaco, /servicos currently 404. Each page needs a route file so navigation works end-to-end. Placeholder pages maintain premium identity while indicating content is coming.

## Output
- `src/app/portfolio/page.tsx` — Portfolio page placeholder
- `src/app/cursos/page.tsx` — Courses page placeholder
- `src/app/espaco/page.tsx` — Space page placeholder
- `src/app/servicos/page.tsx` — Services page placeholder
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Prior Work
@.paul/phases/01-navigation-site-structure/01-01-SUMMARY.md (navbar needs these routes to not 404)

## Source Files
@src/app/layout.tsx (shared layout with Navbar + Footer)
@src/app/page.tsx (homepage pattern reference)
@src/app/aluguel/page.tsx (existing secondary page pattern)
</context>

<acceptance_criteria>

## AC-1: All Routes Resolve
```gherkin
Given the user clicks any navbar link
When the page loads
Then the corresponding page renders without 404
And the Navbar and Footer are visible
And the active link is highlighted in the navbar
```

## AC-2: Placeholder Pages Have Branded Design
```gherkin
Given the user visits a placeholder page
When the page renders
Then it shows:
  - The page title (e.g., "Portfolio", "Cursos")
  - A brief "Em breve" message
  - A CTA button back to homepage or contact
  - Design consistent with Str8Global premium identity
```

## AC-3: Per-Page SEO Metadata
```gherkin
Given any placeholder page
When a search engine crawls it
Then it has unique title, description, and OpenGraph metadata
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create all placeholder pages with metadata</name>
  <files>src/app/portfolio/page.tsx, src/app/cursos/page.tsx, src/app/espaco/page.tsx, src/app/servicos/page.tsx</files>
  <action>
    Create 4 page files following a consistent placeholder pattern:

    Each page:
    - Server component (no 'use client' — metadata export needs server component)
    - Export metadata: unique title ("Portfolio — Str8Global"), description, openGraph
    - Import a shared PlaceholderContent client component OR inline simple JSX
    - Content: centered layout with page title (h1), "Em breve" subtitle, accent gradient glow behind text, CTA link to homepage
    - Minimal — these are temporary until each phase builds the real page

    Page-specific metadata:
    - /portfolio: "Portfolio — Str8Global", "Trabalhos de fotografia e vídeo do Igor e da Marta."
    - /cursos: "Cursos — Str8Global", "Cursos de marketing, fotografia e conteúdo digital."
    - /espaco: "Espaço — Str8Global", "Conheça o nosso espaço criativo em Lisboa."
    - /servicos: "Serviços — Str8Global", "Serviços de marketing, fotografia e vídeo premium."

    Pattern for placeholder content (in each page):
    - min-h-screen flex items-center justify-center
    - text-center with accent glow radial gradient behind
    - h1 with page name, p with "Em breve — estamos a preparar algo especial."
    - Link button "Voltar ao Início" → /

    Avoid:
    - 'use client' on pages that export metadata
    - Importing heavy animation components
    - Creating a separate shared component file (keep it simple, inline)
  </action>
  <verify>
    npm run build — all 4 new routes appear in build output
    All navbar links resolve without 404
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/app/layout.tsx (already integrated with Navbar + Footer)
- src/app/page.tsx (homepage)
- src/app/aluguel/page.tsx (rental page)
- src/components/* (all existing components)
- src/data/* (all existing data)

## SCOPE LIMITS
- Placeholder pages only — no real content implementation
- No new components created
- No data files modified
- These pages will be replaced in Phases 3-6

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` shows all 6 routes (/, /aluguel, /portfolio, /cursos, /espaco, /servicos)
- [ ] No TypeScript errors
- [ ] Each page has unique metadata
- [ ] Navbar links all resolve
</verification>

<success_criteria>
- All 4 placeholder pages created
- Build passes clean with all routes
- Navbar navigation fully functional end-to-end
</success_criteria>

<output>
After completion, create `.paul/phases/01-navigation-site-structure/01-02-SUMMARY.md`
</output>
