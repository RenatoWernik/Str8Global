---
phase: 10-text-animations-section-extraction
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/components/sections/espaco/ManifestoSection.tsx
  - src/components/sections/espaco/EstudiosSection.tsx
  - src/components/sections/espaco/CoworkSection.tsx
  - src/components/sections/espaco/ComodidadesSection.tsx
  - src/components/sections/espaco/index.ts
autonomous: true

must_haves:
  truths:
    - "Manifesto section exists as standalone component with BlurText word-level reveal"
    - "Estudios section exists as standalone component with Masonry grid"
    - "Cowork section exists as standalone component with SpotlightCard grid"
    - "Comodidades section exists as standalone component with hover cards"
    - "Each section manages its own scroll/viewport observation independently"
    - "Section titles use CharReveal for character-by-character animation"
    - "Section subtitles use BalancedHeadline for balanced typography"
  artifacts:
    - path: "src/components/sections/espaco/ManifestoSection.tsx"
      provides: "Manifesto section with BlurText"
      exports: ["ManifestoSection"]
    - path: "src/components/sections/espaco/EstudiosSection.tsx"
      provides: "Studios section with Masonry grid and CharReveal title"
      exports: ["EstudiosSection"]
    - path: "src/components/sections/espaco/CoworkSection.tsx"
      provides: "Cowork section with SpotlightCard grid and CharReveal title"
      exports: ["CoworkSection"]
    - path: "src/components/sections/espaco/ComodidadesSection.tsx"
      provides: "Amenities section with hover cards and CharReveal title"
      exports: ["ComodidadesSection"]
    - path: "src/components/sections/espaco/index.ts"
      provides: "Barrel export for all espaco sections"
      exports: ["ManifestoSection", "EstudiosSection", "CoworkSection", "ComodidadesSection"]
  key_links:
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/data/espacoData.ts"
      via: "import"
      pattern: "import.*espacoData"
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/components/animations/CharReveal.tsx"
      via: "CharReveal component for title"
      pattern: "<CharReveal"
    - from: "src/components/sections/espaco/ManifestoSection.tsx"
      to: "src/components/animations/BlurText.tsx"
      via: "BlurText component"
      pattern: "<BlurText"
---

<objective>
Extract all 4 middle sections (Manifesto, Estudios, Cowork, Comodidades) from espaco/page.tsx into standalone component files, integrating CharReveal and BalancedHeadline from Plan 01.

Purpose: Establishes component architecture (ARCH-01) with loose coupling (ARCH-04) and integrates text animations (TEXT-01, TEXT-02, TEXT-03). Each section becomes independently loadable.
Output: 4 section components + barrel export, ready for dynamic import in Plan 03.
</objective>

<execution_context>
@C:/Users/renat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/renat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-text-animations-section-extraction/10-01-SUMMARY.md
@src/app/espaco/page.tsx
@src/components/animations/BlurText.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManifestoSection and EstudiosSection components</name>
  <files>
    src/components/sections/espaco/ManifestoSection.tsx
    src/components/sections/espaco/EstudiosSection.tsx
  </files>
  <action>
Create directory `src/components/sections/espaco/`.

**ManifestoSection.tsx** — Extract lines 99-108 from page.tsx:
- `'use client'` directive
- Import BlurText from `@/components/animations/BlurText`
- Import `espacoContent` from `@/data/espacoData`
- Named export `ManifestoSection`
- Render the exact same JSX: `<section className="relative py-24 px-6 border-b border-white/5 bg-black z-20">` wrapping a centered `<BlurText>` with the manifesto text from `espacoContent.manifesto`
- BlurText already handles word-level stagger reveal (TEXT-02) and is Framer Motion based — this is correct per LIBRARY-01 (component interaction)
- No props needed — this section is self-contained

**EstudiosSection.tsx** — Extract lines 110-146 from page.tsx:
- `'use client'` directive
- Import: `motion` from framer-motion, `Image` from next/image, `Masonry` from `@/components/ui/Masonry`, `CharReveal` from `@/components/animations/CharReveal`, `BalancedHeadline` from `@/components/animations/BalancedHeadline`
- Import: `estudiosImages`, `espacoContent` from `@/data/espacoData`
- Named export `EstudiosSection`
- Replace the plain `<h2>` title with: `<CharReveal text={espacoContent.estudios.title} tag="h2" className="text-3xl md:text-5xl font-bold mb-4" />` (TEXT-01)
- Wrap the subtitle `<p>` with `<BalancedHeadline as="p" className="text-white/60 text-lg max-w-2xl">` (TEXT-03)
- Keep the Masonry grid + mobile fallback EXACTLY as current (same classes, same Image props, same motion.div animations)
- The section manages its own viewport observation via the existing `whileInView` on each motion.div (ARCH-04 loose coupling)
- Add `loading="eager"` to all Image components inside this section (Pitfall 6 from STATE.md — image reveal timing)

IMPORTANT: Keep the EXACT same visual layout and structure. Only changes are: (1) CharReveal on title, (2) BalancedHeadline on subtitle, (3) data from imports, (4) loading="eager" on images.
  </action>
  <verify>
- Directory src/components/sections/espaco/ exists
- ManifestoSection.tsx exports ManifestoSection, imports BlurText and espacoContent
- EstudiosSection.tsx exports EstudiosSection, imports CharReveal, BalancedHeadline, estudiosImages
- EstudiosSection.tsx contains `loading="eager"` on Image components
- Both files have 'use client' directive
- `npm run build` completes without errors (components not yet used in page — that's Plan 03)
  </verify>
  <done>ManifestoSection renders word-level blur reveal via BlurText. EstudiosSection renders Masonry grid with CharReveal title and BalancedHeadline subtitle. Both are self-contained with data imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create CoworkSection and ComodidadesSection components + barrel export</name>
  <files>
    src/components/sections/espaco/CoworkSection.tsx
    src/components/sections/espaco/ComodidadesSection.tsx
    src/components/sections/espaco/index.ts
  </files>
  <action>
**CoworkSection.tsx** — Extract lines 148-180 from page.tsx:
- `'use client'` directive
- Import: `motion` from framer-motion, `Image` from next/image, `SpotlightCard` from `@/components/ui/SpotlightCard`, `CharReveal` from `@/components/animations/CharReveal`, `BalancedHeadline` from `@/components/animations/BalancedHeadline`
- Import: `coworkImages`, `espacoContent` from `@/data/espacoData`
- Named export `CoworkSection`
- Replace `<h2>` with `<CharReveal text={espacoContent.cowork.title} tag="h2" className="text-3xl md:text-5xl font-bold mb-4" />`
- Wrap subtitle with `<BalancedHeadline as="p" className="text-white/60 text-lg max-w-2xl ml-auto">`
- Keep the grid layout, SpotlightCard, motion.div animations EXACTLY as current
- Add `loading="eager"` to all Image components
- Section manages own viewport via existing whileInView (ARCH-04)

**ComodidadesSection.tsx** — Extract lines 182-217 from page.tsx:
- `'use client'` directive
- Import: `motion` from framer-motion, `Image` from next/image, `CharReveal` from `@/components/animations/CharReveal`, `BalancedHeadline` from `@/components/animations/BalancedHeadline`
- Import: `comodidadesImages`, `espacoContent` from `@/data/espacoData`
- Named export `ComodidadesSection`
- Replace `<h2>` with `<CharReveal text={espacoContent.comodidades.title} tag="h2" className="text-3xl md:text-5xl font-bold mb-4" />`
- Wrap subtitle with `<BalancedHeadline as="p" className="text-white/60 text-lg max-w-2xl mx-auto">`
- Keep the grid + hover card layout EXACTLY as current
- Add `loading="eager"` to all Image components

**index.ts** — Barrel export:
```typescript
export { ManifestoSection } from './ManifestoSection';
export { EstudiosSection } from './EstudiosSection';
export { CoworkSection } from './CoworkSection';
export { ComodidadesSection } from './ComodidadesSection';
```

IMPORTANT: All 4 sections must keep their EXACT current layout, classes, and structure. The ONLY changes are: CharReveal on titles, BalancedHeadline on subtitles, data from espacoData imports, and loading="eager" on images. The creative visual redesign happens in Phase 11.
  </action>
  <verify>
- CoworkSection.tsx exports CoworkSection, imports CharReveal, SpotlightCard, coworkImages
- ComodidadesSection.tsx exports ComodidadesSection, imports CharReveal, comodidadesImages
- Both files have loading="eager" on Image components
- index.ts re-exports all 4 section components
- `npm run build` completes without errors
  </verify>
  <done>All 4 espaco sections exist as standalone components with CharReveal titles, BalancedHeadline subtitles, data from espacoData.ts, and barrel export. Each manages own viewport observation independently.</done>
</task>

</tasks>

<verification>
- All 4 section files exist in src/components/sections/espaco/
- Each section imports its data from espacoData.ts (not inline)
- Each section uses CharReveal for title animation
- Each section uses BalancedHeadline for subtitle wrapping
- Each section has 'use client' and loading="eager" on Images
- index.ts barrel exports all 4
- `npm run build` passes
</verification>

<success_criteria>
- 4 standalone section components faithfully reproduce current page sections
- CharReveal integrated on all 3 section titles (Estudios, Cowork, Comodidades)
- BalancedHeadline integrated on all 3 section subtitles
- ManifestoSection uses existing BlurText for word-level reveal
- All data comes from espacoData.ts imports
- Sections are independently usable (loose coupling per ARCH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/10-text-animations-section-extraction/10-02-SUMMARY.md`
</output>
