---
phase: 11-visual-redesign-image-effects
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/sections/espaco/EstudiosSection.tsx
autonomous: true

must_haves:
  truths:
    - "Studios section uses CSS Grid with explicit named template areas creating asymmetric bento layout"
    - "All 5 studio images reveal with clip-path animation when section enters viewport"
    - "Section uses single IntersectionObserver via useSectionInView (not per-image whileInView)"
    - "Layout is visually distinct from standard grid — areas have varying sizes"
    - "Mobile layout degrades gracefully to stacked single-column"
  artifacts:
    - path: "src/components/sections/espaco/EstudiosSection.tsx"
      provides: "Bento grid studios section with GSAP clip-path reveals"
      min_lines: 50
  key_links:
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/components/animations/RevealImage.tsx"
      via: "import and render RevealImage"
      pattern: "import.*RevealImage"
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/hooks/useSectionInView.ts"
      via: "useSectionInView hook"
      pattern: "useSectionInView"
---

<objective>
Rewrite EstudiosSection with an asymmetric bento grid layout (CSS Grid named areas) and GSAP clip-path image reveals via RevealImage.

Purpose: VFX-02 requires a creative bento grid layout with asymmetric areas. This replaces the current Masonry layout with a more visually impactful CSS Grid where images have varying sizes, creating visual hierarchy.

Output: Rewritten EstudiosSection.tsx
</objective>

<execution_context>
@C:/Users/renat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/renat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visual-redesign-image-effects/11-01-SUMMARY.md
@src/components/sections/espaco/EstudiosSection.tsx
@src/data/espacoData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite EstudiosSection with bento grid and RevealImage</name>
  <files>src/components/sections/espaco/EstudiosSection.tsx</files>
  <action>
Rewrite `src/components/sections/espaco/EstudiosSection.tsx` completely.

**Remove:**
- `import { motion } from 'framer-motion'` — no more Framer Motion in this section
- `import { Masonry }` — replaced by CSS Grid
- All `motion.div` wrappers with `whileInView` (VFX-06 — no per-image observers)

**Add:**
- `import { RevealImage } from '@/components/animations/RevealImage'`
- `import { useSectionInView } from '@/hooks/useSectionInView'`
- Keep: CharReveal, BalancedHeadline, espacoData imports

**Layout — Desktop Bento Grid (5 images, CSS Grid):**

Use CSS Grid with explicit `grid-template-areas` for a creative asymmetric layout:

```
Desktop layout (md+):
┌──────────┬──────────┐
│          │   img2   │
│   img1   ├──────────┤
│  (tall)  │   img3   │
├──────┬───┴──────────┤
│ img4 │    img5      │
│      │   (wide)     │
└──────┴──────────────┘
```

Implementation with Tailwind classes:
- Container: `grid grid-cols-1 md:grid-cols-12 md:grid-rows-[300px_300px_280px] gap-3`
- Image 1 (Estudio1): `md:col-span-7 md:row-span-2` — tall left hero image, aspect auto via grid row heights
- Image 2 (Estudio2): `md:col-span-5 md:row-span-1` — top right
- Image 3 (Estudio-Podcast): `md:col-span-5 md:row-span-1` — middle right
- Image 4 (Estudio-Podcast2): `md:col-span-4 md:row-span-1` — bottom left, shorter
- Image 5 (Estudio-Podcast3): `md:col-span-8 md:row-span-1` — bottom right, wide

Each grid cell: `relative overflow-hidden rounded-xl border border-white/5`

**Mobile layout:**
- Single column with `grid-cols-1`, each image full width with varying aspect ratios:
  - Image 1: `aspect-[4/5]` (portrait)
  - Images 2-3: `aspect-video` (16:9)
  - Images 4-5: `aspect-[3/2]` (landscape)

**Section-level IntersectionObserver:**
- Call `const { sectionRef, isInView } = useSectionInView()`
- Attach `sectionRef` to the `<section>` element
- Pass `isInView` to each RevealImage

**RevealImage usage per image:**
```tsx
<RevealImage
  src={estudiosImages[0]}
  alt="Estúdio 1"
  isInView={isInView}
  delay={0}            // First image, no delay
  direction="up"       // img1 reveals upward
  className="relative w-full h-full overflow-hidden rounded-xl border border-white/5"
/>
```
Stagger delays: img1=0, img2=0.1, img3=0.2, img4=0.15, img5=0.25
Directions: img1='up', img2='left', img3='left', img4='up', img5='right'

**Keep intact:**
- CharReveal title with same text and classes
- BalancedHeadline subtitle
- Section padding, background color (#030303), z-index
- All 5 estudiosImages used, loading="eager" (handled by RevealImage)
  </action>
  <verify>
- `npx tsc --noEmit` passes
- `npm run build` passes
- File has NO `import { motion }` from framer-motion
- File has NO `whileInView` prop
- File imports RevealImage and useSectionInView
- File has `grid-template` or `grid-cols-12` (CSS Grid, not Masonry)
- All 5 estudiosImages are rendered
  </verify>
  <done>EstudiosSection uses asymmetric bento grid CSS layout with 5 images of varying sizes, all revealed via GSAP clip-path through single section-level IntersectionObserver</done>
</task>

</tasks>

<verification>
1. `npm run build` — no errors
2. EstudiosSection renders 5 images in bento grid
3. No Framer Motion imports in EstudiosSection
4. Single useSectionInView call (1 observer, not 5)
5. All images use RevealImage with clip-path animation
</verification>

<success_criteria>
- Studios section displays 5 images in asymmetric bento grid (not uniform Masonry columns)
- CSS Grid uses explicit areas with varying cell sizes
- All images animate with clip-path reveal (not fade)
- Only 1 IntersectionObserver for the entire section
- Mobile degrades to single column with appropriate aspect ratios
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-redesign-image-effects/11-02-SUMMARY.md`
</output>
