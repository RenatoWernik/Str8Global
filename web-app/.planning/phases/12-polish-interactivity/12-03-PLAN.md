---
phase: 12-polish-interactivity
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - src/components/sections/espaco/EstudiosSection.tsx
  - src/components/sections/espaco/CoworkSection.tsx
  - src/components/sections/espaco/ComodidadesSection.tsx
autonomous: true

must_haves:
  truths:
    - "2-3 select images in Estudios section have subtle parallax scroll effect on desktop"
    - "Clicking any image in any section opens fullscreen lightbox with smooth morph transition"
    - "Interactive image containers have magnetic cursor pull effect on desktop"
    - "Mobile users see NO parallax, NO magnetic cursor — images display as static with existing clip-path reveals only"
    - "All existing RevealImage clip-path animations continue to work correctly alongside new effects"
    - "Build compiles with zero TypeScript errors"
  artifacts:
    - path: "src/components/sections/espaco/EstudiosSection.tsx"
      provides: "Estudios section with parallax on 2 hero images + lightbox + magnetic cursor"
      contains: "ParallaxImage"
    - path: "src/components/sections/espaco/CoworkSection.tsx"
      provides: "Cowork section with lightbox on all images + magnetic cursor"
      contains: "ImageLightbox"
    - path: "src/components/sections/espaco/ComodidadesSection.tsx"
      provides: "Comodidades section with lightbox on all images + magnetic cursor"
      contains: "ImageLightbox"
  key_links:
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/components/animations/ParallaxImage.tsx"
      via: "import and render for hero images"
      pattern: "import.*ParallaxImage"
    - from: "src/components/sections/espaco/EstudiosSection.tsx"
      to: "src/components/ui/ImageLightbox.tsx"
      via: "import and render for fullscreen view"
      pattern: "import.*ImageLightbox"
    - from: "src/components/sections/espaco/CoworkSection.tsx"
      to: "src/components/ui/ImageLightbox.tsx"
      via: "import and render"
      pattern: "import.*ImageLightbox"
    - from: "src/components/sections/espaco/ComodidadesSection.tsx"
      to: "src/components/ui/ImageLightbox.tsx"
      via: "import and render"
      pattern: "import.*ImageLightbox"
---

<objective>
Integrate ParallaxImage, ImageLightbox, and MagneticCursor into the three Espaco section components. Apply parallax to 2-3 select images in Estudios (largest/most impactful), add lightbox click-to-view to all images across all sections, and wrap image containers with MagneticCursor on desktop. Verify mobile gets simplified experience and build compiles cleanly.

Purpose: Complete the polish layer by wiring all new effects into existing sections while maintaining mobile performance and accessibility.
Output: Three updated section components with full polish effects + verified build.
</objective>

<execution_context>
@C:/Users/renat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/renat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-interactivity/12-01-SUMMARY.md
@.planning/phases/12-polish-interactivity/12-02-SUMMARY.md

@src/components/sections/espaco/EstudiosSection.tsx
@src/components/sections/espaco/CoworkSection.tsx
@src/components/sections/espaco/ComodidadesSection.tsx
@src/components/animations/ParallaxImage.tsx
@src/components/ui/ImageLightbox.tsx
@src/components/ui/MagneticCursor.tsx
@src/components/animations/RevealImage.tsx
@src/data/espacoData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate parallax + lightbox + magnetic cursor into EstudiosSection</name>
  <files>src/components/sections/espaco/EstudiosSection.tsx</files>
  <action>
Modify EstudiosSection to add parallax, lightbox, and magnetic cursor effects.

**Parallax (2 images only — the most impactful):**
- Replace the 2 largest bento grid images (Image 1: col-span-7 row-span-2, Image 5: col-span-8) with `ParallaxImage` instead of `RevealImage`. These are the hero images that benefit most from parallax.
- Use `intensity={10}` (10% movement, well under 15% cap).
- Keep the remaining 3 images as `RevealImage` (parallax on ALL images would be too busy).
- ParallaxImage gets the same `className` and styling as the RevealImage it replaces.

**Lightbox (all 5 desktop images):**
- Add `useState<number | null>(null)` for `selectedImage` index.
- Each image container (both ParallaxImage and RevealImage) gets:
  - `layoutId={`estudios-${idx}`}` prop
  - `onClick={() => setSelectedImage(idx)}`
  - `className="cursor-pointer"` added to container
- Render `ImageLightbox` at the bottom of the section (inside the section, after the grid):
  ```tsx
  {selectedImage !== null && (
    <ImageLightbox
      src={estudiosImages[selectedImage]}
      alt={`Estúdio ${selectedImage + 1}`}
      layoutId={`estudios-${selectedImage}`}
      isOpen={selectedImage !== null}
      onClose={() => setSelectedImage(null)}
    />
  )}
  ```

**Magnetic cursor (desktop image containers):**
- Wrap each desktop grid cell `div` (the ones with `md:col-span-*`) with `MagneticCursor` with `strength={0.15}` (subtle — these are large elements).
- MagneticCursor auto-disables on mobile, so no conditional needed.

**Mobile layout:** NO changes to the mobile layout block (`flex md:hidden`). Mobile images stay as plain `Image` components with no parallax, no lightbox, no magnetic cursor. Add lightbox support to mobile images too (just onClick + ImageLightbox, no parallax/magnetic).

**Preserve existing behavior:**
- `useSectionInView` and `isInView` prop passing remain unchanged.
- RevealImage clip-path animations on the 3 non-parallax images remain unchanged.
- CharReveal and BalancedHeadline remain unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors.
Verify file imports ParallaxImage, ImageLightbox, MagneticCursor.
Verify exactly 2 ParallaxImage instances and 3 RevealImage instances in desktop grid.
Verify selectedImage state exists.
Verify mobile block has NO ParallaxImage or MagneticCursor.
  </verify>
  <done>EstudiosSection has 2 parallax hero images, lightbox on all images, magnetic cursor on desktop containers, mobile unchanged except lightbox support. All existing clip-path reveals preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate lightbox + magnetic cursor into CoworkSection and ComodidadesSection</name>
  <files>
    src/components/sections/espaco/CoworkSection.tsx
    src/components/sections/espaco/ComodidadesSection.tsx
  </files>
  <action>
Modify both sections to add lightbox and magnetic cursor effects. NO parallax in these sections (parallax is only for Estudios per POLISH-01 "2-3 hero images").

**CoworkSection changes:**
- Add `useState<number | null>(null)` for `selectedImage`.
- Each of the 5 staggered image containers gets:
  - `layoutId={`cowork-${idx}`}` on the RevealImage
  - `onClick={() => setSelectedImage(idx)}` on the outer container div
  - `className="cursor-pointer"` on the clickable container
- Wrap each image container (the `w-full md:w-[XX%]` divs) with `MagneticCursor` with `strength={0.12}`.
- Render `ImageLightbox` at end of section:
  ```tsx
  {selectedImage !== null && (
    <ImageLightbox
      src={coworkImages[selectedImage].src}
      alt={coworkImages[selectedImage].title}
      layoutId={`cowork-${selectedImage}`}
      isOpen={selectedImage !== null}
      onClose={() => setSelectedImage(null)}
    />
  )}
  ```

**ComodidadesSection changes:**
- Add `useState<number | null>(null)` for `selectedImage`.
- Each of the 3 grid items gets:
  - `layoutId={`comodidades-${idx}`}` on the RevealImage
  - `onClick={() => setSelectedImage(idx)}` on the outer `group` div
  - `className="cursor-pointer"` on the clickable container
- Wrap each grid cell with `MagneticCursor` with `strength={0.15}`.
- Render `ImageLightbox` at end of section with `comodidadesImages[selectedImage].src`.

**Both sections — preserve existing behavior:**
- All RevealImage components keep their existing props (isInView, delay, direction, className, imageClassName).
- Title overlays, gradients, hover effects remain unchanged.
- `useSectionInView` pattern unchanged.
- CharReveal and BalancedHeadline unchanged.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors.
Verify CoworkSection imports ImageLightbox and MagneticCursor.
Verify ComodidadesSection imports ImageLightbox and MagneticCursor.
Verify both have selectedImage state.
Neither file should import ParallaxImage (parallax is Estudios-only).
  </verify>
  <done>CoworkSection and ComodidadesSection have lightbox on all images and magnetic cursor on desktop containers. No parallax added (Estudios-only). All existing layouts, overlays, and animations preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification and mobile safety check</name>
  <files></files>
  <action>
Run full build verification to ensure all changes compile and no regressions exist.

**Build check:**
- Run `npx tsc --noEmit` — must pass with 0 errors.
- Run `npm run build` — must complete successfully.

**Code quality checks (grep-based):**
- Verify NO `console.log` in any modified/new file.
- Verify NO `TODO` or `FIXME` in any modified/new file.
- Verify `prefers-reduced-motion` is handled in all 3 new components (ParallaxImage, ImageLightbox, MagneticCursor).
- Verify mobile detection (`matchMedia.*768` or `max-width.*767`) exists in ParallaxImage and MagneticCursor.
- Verify `loading="eager"` exists in ParallaxImage (VFX-05).

**Mobile safety verification (static analysis):**
- Verify EstudiosSection mobile block (`flex md:hidden`) has NO ParallaxImage import usage.
- Verify MagneticCursor component has mobile bypass (renders children directly).
- Count total IntersectionObservers: should still be 3 (one per section via useSectionInView). No new observers added.

**If any check fails:** Fix the issue in the relevant file before completing.
  </action>
  <verify>
`npm run build` completes successfully.
`npx tsc --noEmit` returns 0 errors.
Grep confirms no console.log, no TODO/FIXME in new/modified files.
  </verify>
  <done>Full build passes, TypeScript compiles cleanly, all quality checks pass, mobile safety confirmed (no parallax/magnetic on mobile), IntersectionObserver count unchanged at 3.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes successfully
2. `npx tsc --noEmit` returns 0 errors
3. EstudiosSection has 2 ParallaxImage + 3 RevealImage + ImageLightbox + MagneticCursor
4. CoworkSection has 5 RevealImage + ImageLightbox + MagneticCursor (no ParallaxImage)
5. ComodidadesSection has 3 RevealImage + ImageLightbox + MagneticCursor (no ParallaxImage)
6. All 3 new components respect prefers-reduced-motion
7. ParallaxImage and MagneticCursor disabled on mobile
8. No console.log, TODO, or FIXME in any modified file
9. IntersectionObserver count remains at 3 (no new observers)
</verification>

<success_criteria>
- Estudios section: 2 hero images have parallax on desktop, all images open lightbox, magnetic cursor on containers
- Cowork section: all images open lightbox, magnetic cursor on containers, no parallax
- Comodidades section: all images open lightbox, magnetic cursor on containers, no parallax
- Mobile: no parallax, no magnetic cursor, lightbox works (click to fullscreen)
- prefers-reduced-motion: all effects disabled/instant
- Build compiles with 0 errors
- Phase 12 complete — milestone v1.3 SHIPPED
</success_criteria>

<output>
After completion, create `.planning/phases/12-polish-interactivity/12-03-SUMMARY.md`
</output>
