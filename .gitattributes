# Intentionally empty: video files (*.mp4/*.mov) are NOT tracked via Git LFS.
# They are stored as regular binary blobs so Vercel deployments can serve them
# without depending on GitHub's LFS bandwidth budget.
